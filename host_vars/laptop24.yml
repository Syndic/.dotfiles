---
# Host-specific variables for laptop24.
# macOS defaults overrides for this machine go here.

# Brewfile with packages specific to this machine (layered on top of the
# common and group Brewfiles).
brewfile_host: "{{ playbook_dir }}/brewfiles/hosts/laptop24.Brewfile"
