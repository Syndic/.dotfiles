# Lint tooling for the `~/.venv-ansible` devcontainer venv (post-create.sh)
# and the `lint` CI job (tests.yml). Pinned and Renovate-managed — see
# CLAUDE.md "Dependency updates". ansible-lint pulls ansible-core as a
# dependency; the pinned ansible-core lives in molecule-requirements.txt,
# which shares the venv.
ansible-lint==26.8.0
yamllint==1.38.0
