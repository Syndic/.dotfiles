repos:
  - repo: local
    hooks:
      - id: dotfiles-source-guard
        name: prevent recursive dotfiles repo nesting
        entry: python3 dotfiles_manager.py check home_source
        language: system
        pass_filenames: false
        always_run: true
