repos:
  - repo: local
    hooks:
      - id: dotfiles-source-guard
        name: dotfiles source guard
        entry: python3 dotfiles_tree.py check home_source
        language: system
        pass_filenames: false
        always_run: true
