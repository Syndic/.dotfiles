---
# certs role
# Will: trust personal CA roots in the system keychain (requires sudo + security add-trusted-cert),
# request machine certs from smallstep CA, and provision SSH host/user certs.
# Must be idempotent and independently invokable via --tags certs.

- name: certs role placeholder
  ansible.builtin.debug:
    msg: "certs role: not yet implemented (smallstep CA planned)"
  tags: certs
